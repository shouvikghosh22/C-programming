
#include <stdio.h>
#include <math.h>

int main() {
float n,cube,root;
printf ("Enter a number: ");
scanf ("%f", &n);

cube=pow(n,3);
root=sqrt(n);

printf("\nCube is: %f", cube);
printf("\nRoot is: %f", root);


}
